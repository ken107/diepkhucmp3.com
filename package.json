{
  "scripts": {
    "build": "node build.js build",
    "start": "http-server -p 80 build",
    "deploy": "aws s3 sync --profile=s3 --delete --metadata-directive REPLACE --cache-control max-age=1800,public web s3://music.diepkhuc.com"
  },
  "devDependencies": {
    "uglify-js": "^3.3.3"
  }
}
